type ServerSidePropsType = {
  params: { date?: string[] };
  searchParams: Record<string, string>;
};

const ProfilePage = (props: ServerSidePropsType) => {
  return <div>image</div>;
};
export default ProfilePage;
